{
  "name": "my-static-site",
  "version": "1.0.0",
  "scripts": {
    "start": "serve -s . -l $PORT",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "serve": "^14.2.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
